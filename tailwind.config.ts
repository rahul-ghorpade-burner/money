// Tailwind CSS v4: design tokens are defined in src/index.css via @theme { }
// The Vite plugin is configured in vite.config.ts via @tailwindcss/vite
// Do NOT add v3-style theme config here — it has no effect in v4
